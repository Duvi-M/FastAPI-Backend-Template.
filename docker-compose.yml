services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app
    ports: ["5433:5432"]
  api:
    build: .
    env_file: .env
    ports: ["8001:8001"]
    depends_on: [db]
